# Database Configuration
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

DATABASE_URL=

# CORS Configuration (Comma-separated list of allowed origins or * for all)
CORS_ORIGINS=

# Application Base URL (เปลี่ยนเป็น Domain ที่ใช้งานจริง เช่น https://zk-ml.yeedev.asia เมื่อ Deploy ขึ้น Production)
APP_BASE_URL=

# File Encryption Key (AES-256 Fernet key for encrypting uploaded PDFs & docs at rest)
FILE_ENCRYPTION_KEY=
